export const PORT = 4000
export const HOST_URL = `http://localhost:${PORT}`
export const SG_API_KEY = process.env.SG_API_KEY
export const midiDir = `${process.cwd()}/midis/`
export const instrumentsDir = `${process.cwd()}/instruments/`